const cart=[];const cartEl=document.getElementById('cart');const overlay=document.getElementById('overlay');const itemsEl=document.getElementById('cartItems');const countEl=document.getElementById('cartCount');const subtotalEl=document.getElementById('subtotal');
function openCart(){cartEl.classList.add('open');overlay.classList.add('show')}function closeCart(){cartEl.classList.remove('open');overlay.classList.remove('show')}
document.getElementById('cartOpen').onclick=openCart;document.getElementById('cartClose').onclick=closeCart;overlay.onclick=closeCart;
document.querySelectorAll('.quick').forEach(btn=>btn.addEventListener('click',e=>{const p=e.target.closest('.product');const item={name:p.dataset.name,price:+p.dataset.price,image:p.dataset.image};cart.push(item);renderCart();openCart()}));
function renderCart(){countEl.textContent=cart.length;if(!cart.length){itemsEl.innerHTML='<p class="empty">Your bag is empty.</p>';subtotalEl.textContent='₹0';return}itemsEl.innerHTML=cart.map((x,i)=>`<div class="cart-row"><img src="${x.image}" alt=""><div><h4>${x.name}</h4><p>₹${x.price.toLocaleString('en-IN')}</p></div><button onclick="removeItem(${i})">Remove</button></div>`).join('');const total=cart.reduce((a,x)=>a+x.price,0);subtotalEl.textContent='₹'+total.toLocaleString('en-IN')}
function removeItem(i){cart.splice(i,1);renderCart()}document.getElementById('checkout').onclick=()=>alert('Demo checkout: connect Razorpay, Stripe, Shopify, or your backend here.');
document.getElementById('newsletter').addEventListener('submit',e=>{e.preventDefault();document.getElementById('formMsg').textContent='You’re in. Welcome to the Hub ✦';e.target.reset()});
